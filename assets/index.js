/**
 * Skunkworks Capability Portfolio - Main Bundle
 * This file is normally generated by Vite, but is provided as a fallback
 */

// Import React and ReactDOM (this is done dynamically in the actual bundle)
import * as React from 'https://cdn.jsdelivr.net/npm/react@18.2.0/+esm';
import * as ReactDOM from 'https://cdn.jsdelivr.net/npm/react-dom@18.2.0/+esm';

// Create a simple App component
const App = () => {
  return React.createElement('div', { className: 'container my-5' },
    React.createElement('h1', null, 'Skunkworks Capability Portfolio'),
    React.createElement('div', { className: 'row' },
      React.createElement('div', { className: 'col-md-6' },
        React.createElement('div', { className: 'card' },
          React.createElement('div', { className: 'card-body' },
            React.createElement('h5', { className: 'card-title' }, 'Capabilities'),
            React.createElement('p', { className: 'card-text' }, 'Showcasing innovative projects and advanced capabilities.')
          )
        )
      ),
      React.createElement('div', { className: 'col-md-6' },
        React.createElement('div', { className: 'card' },
          React.createElement('div', { className: 'card-body' },
            React.createElement('h5', { className: 'card-title' }, 'Projects'),
            React.createElement('p', { className: 'card-text' }, 'Explore our cutting-edge technological solutions.')
          )
        )
      )
    )
  );
};

// Render the app when the DOM is loaded
document.addEventListener('DOMContentLoaded', () => {
  const rootElement = document.getElementById('root');
  if (rootElement) {
    // Clear the loading screen
    rootElement.innerHTML = '';
    
    // Render the React app
    const root = ReactDOM.createRoot(rootElement);
    root.render(React.createElement(React.StrictMode, null, React.createElement(App)));
    
    console.log('Skunkworks Capability Portfolio loaded successfully');
  } else {
    console.error('Root element not found');
  }
});

// Log when the script loads
console.log('Skunkworks main bundle loaded');
